CC = gcc
ouput = interpreter

main:		
		$(CC) -o ArduinoDriver.o -c ArduinoDriver.c	
		$(CC) -o deviceLibrary.o -c deviceLibrary.c 
		$(CC) -std=gnu99 -o $(ouput) interpreter.c ArduinoDriver.c deviceLibrary.o

clean:
		rm deviceLibrary.o
		rm $(ouput)




